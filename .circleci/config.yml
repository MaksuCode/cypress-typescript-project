version: 2.1

parameters:
  browser:
    type: string
    default: "chrome"

jobs:
  build:
    docker:
      - image: cypress/included:12.6.0
    environment:
      BROWSER: << pipeline.parameters.browser >>  
    steps:
      - checkout
      - run: npm install
      - run: npx cypress run --browser ${BROWSER}
workflows:
  build:
    jobs:
      - build
